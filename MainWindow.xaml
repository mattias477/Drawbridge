<Window x:Class="Drawbridge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Drawbridge" Height="760" Width="880"
        Background="#1E2430">

    <Window.Resources>
        <!-- Sidebar navigation item -->
        <Style x:Key="NavItem" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#AAB3C5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="6"
                                Padding="12,10" Margin="8,2">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1E2430"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#252C3B"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buttons -->
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#3B82F6"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#6B7280"/>
        </Style>
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#10B981"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="#DC2626"/>
        </Style>

        <!-- Cards & text -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#252C3B"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
        <Style x:Key="TabTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="21"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
        <Style x:Key="Hint" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#8A93A6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB3C5"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style x:Key="DarkCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D5DCE8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="DarkRadio" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#D5DCE8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="DarkInput" TargetType="TextBox">
            <Setter Property="Background" Value="#141922"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3B4252"/>
            <Setter Property="Padding" Value="7,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="DarkPin" TargetType="PasswordBox">
            <Setter Property="Background" Value="#141922"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3B4252"/>
            <Setter Property="Padding" Value="7,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- ==================== MAIN CONTENT ==================== -->
        <Grid x:Name="MainContent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="190"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ===== Sidebar ===== -->
            <Border Grid.Column="0" Background="#161B26">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top">
                        <StackPanel Orientation="Horizontal" Margin="16,18,0,20">
                            <TextBlock Text="🏰" FontSize="22" FontFamily="Segoe UI Symbol"
                                       Foreground="White" VerticalAlignment="Center"/>
                            <TextBlock Text="Drawbridge" FontSize="18" FontWeight="Bold"
                                       Foreground="White" Margin="8,0,8,0"
                                       VerticalAlignment="Center"/>
                            <Ellipse x:Name="SideStatusLight" Width="9" Height="9"
                                     Fill="IndianRed" VerticalAlignment="Center"/>
                        </StackPanel>

                        <RadioButton Style="{StaticResource NavItem}" Tag="Dashboard"
                                     Content="🏠  Dashboard" IsChecked="True"
                                     Checked="Nav_Checked" GroupName="Nav"/>
                        <RadioButton Style="{StaticResource NavItem}" Tag="Blocklists"
                                     Content="📋  Blocklists" Checked="Nav_Checked" GroupName="Nav"/>
                        <RadioButton Style="{StaticResource NavItem}" Tag="Settings"
                                     Content="⚙️  Settings" Checked="Nav_Checked" GroupName="Nav"/>
                        <RadioButton Style="{StaticResource NavItem}" Tag="Security"
                                     Content="🔒  Security" Checked="Nav_Checked" GroupName="Nav"/>
                        <RadioButton Style="{StaticResource NavItem}" Tag="Logs"
                                     Content="📜  Logs" Checked="Nav_Checked" GroupName="Nav"/>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom" Margin="16,0,16,14"
                                VerticalAlignment="Bottom">
                        <Button x:Name="LockNowButton" Content="🔒 Lock now"
                                Style="{StaticResource SecondaryButton}"
                                Visibility="Collapsed" Margin="0,0,0,10"
                                Click="LockNowButton_Click"/>
                        <TextBlock Text="Drawbridge v1.0" Style="{StaticResource Hint}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                    <Grid/>
                </DockPanel>
            </Border>

            <!-- ===== Content area ===== -->
            <Grid Grid.Column="1" Margin="24,20,24,20">

                <!-- ========== DASHBOARD ========== -->
                <Grid x:Name="TabDashboard">
                    <StackPanel>
                        <TextBlock Text="Dashboard" Style="{StaticResource TabTitle}"/>

                        <Border Style="{StaticResource Card}" Padding="14,12">
                            <DockPanel>
                                <Button x:Name="ToggleButton" Content="Raise the Bridge (Start)"
                                        Style="{StaticResource PrimaryButton}"
                                        DockPanel.Dock="Right" Click="ToggleButton_Click"/>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse x:Name="StatusLight" Width="16" Height="16"
                                             Fill="IndianRed" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="StatusText" FontSize="16"
                                               Text="Bridge is down (not filtering)"
                                               Foreground="White" Margin="10,0,0,0"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="14"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="14"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock x:Name="DomainsLoadedText" Text="0" FontSize="28"
                                               FontWeight="Bold" Foreground="#3B82F6"/>
                                    <TextBlock x:Name="DomainsLoadedLabel"
                                               Text="domains on the blocklist"
                                               Style="{StaticResource Hint}"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock x:Name="TodayBlockedText" Text="0" FontSize="28"
                                               FontWeight="Bold" Foreground="#EF4444"/>
                                    <TextBlock Text="lookups blocked today"
                                               Style="{StaticResource Hint}"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="4" Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock x:Name="AllTimeBlockedText" Text="0" FontSize="28"
                                               FontWeight="Bold" Foreground="#F59E0B"/>
                                    <TextBlock Text="blocked total (90 days kept)"
                                               Style="{StaticResource Hint}"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Blocked lookups — last 14 days" FontSize="14"
                                           FontWeight="SemiBold" Foreground="White"
                                           Margin="0,0,0,12"/>
                                <Grid x:Name="ChartHost" Height="140"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- ========== BLOCKLISTS ========== -->
                <Grid x:Name="TabBlocklists" Visibility="Collapsed">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Blocklists" Style="{StaticResource TabTitle}"/>

                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="Filtering mode" FontSize="15" FontWeight="SemiBold"
                                               Foreground="White" Margin="0,0,0,10"/>

                                    <RadioButton x:Name="BlocklistModeRadio" GroupName="FilterMode"
                                                 Style="{StaticResource DarkRadio}"
                                                 Content="Blocklist mode — allow everything except blocked domains"
                                                 Checked="FilterMode_Changed"/>
                                    <RadioButton x:Name="WhitelistModeRadio" GroupName="FilterMode"
                                                 Style="{StaticResource DarkRadio}"
                                                 Content="Whitelist mode — block everything except allowed domains"
                                                 Checked="FilterMode_Changed"/>

                                    <TextBlock Style="{StaticResource Hint}"
                                               Text="In whitelist mode, ONLY the 'Allowed domains' below (plus Windows system essentials) will work. Everything else — every site, app, and service — is blocked. Best for young children with a short list of approved sites."/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="Allowed domains" FontSize="15" FontWeight="SemiBold"
                                               Foreground="White" Margin="0,0,0,8"/>

                                    <ListBox x:Name="AllowedDomainsList" Height="70" Background="#141922"
                                             Foreground="#9FE8A5" BorderThickness="0" FontSize="12"
                                             Padding="4"/>

                                    <TextBlock Text="Blocklist mode: exceptions that override blocks. Whitelist mode: the only sites that work. Subdomains included."
                                               Style="{StaticResource FieldLabel}" Margin="0,10,0,3"/>
                                    <DockPanel>
                                        <Button x:Name="RemoveAllowedButton" Content="Remove selected"
                                                Style="{StaticResource SecondaryButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="RemoveAllowedButton_Click"/>
                                        <Button x:Name="AddAllowedButton" Content="Allow domain"
                                                Style="{StaticResource SuccessButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="AddAllowedButton_Click"/>
                                        <TextBox x:Name="NewAllowedBox" Style="{StaticResource DarkInput}"
                                                 KeyDown="NewAllowedBox_KeyDown"/>
                                    </DockPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <DockPanel Margin="0,0,0,8">
                                        <TextBlock Text="List sources (blocklist mode)" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="White" DockPanel.Dock="Left"/>
                                        <TextBlock x:Name="BlockedCountText" Text="0 domains loaded"
                                                   Foreground="#AAB3C5" HorizontalAlignment="Right"/>
                                    </DockPanel>

                                    <ListBox x:Name="BlocklistUrls" Height="70" Background="#141922"
                                             Foreground="#D5DCE8" BorderThickness="0" FontSize="12"
                                             Padding="4"/>

                                    <TextBlock Text="Add a new list URL (Adblock format)"
                                               Style="{StaticResource FieldLabel}" Margin="0,10,0,3"/>
                                    <DockPanel>
                                        <Button x:Name="RemoveUrlButton" Content="Remove selected"
                                                Style="{StaticResource SecondaryButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="RemoveUrlButton_Click"/>
                                        <Button x:Name="AddUrlButton" Content="Add list"
                                                Style="{StaticResource PrimaryButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="AddUrlButton_Click"/>
                                        <Button x:Name="CheckUpdatesButton" Content="⟳ Updates"
                                                Style="{StaticResource SuccessButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="CheckUpdatesButton_Click"/>
                                        <TextBox x:Name="NewUrlBox" Style="{StaticResource DarkInput}"/>
                                    </DockPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="Custom block rules (blocklist mode)" FontSize="15" FontWeight="SemiBold"
                                               Foreground="White" Margin="0,0,0,8"/>

                                    <ListBox x:Name="CustomDomainsList" Height="70" Background="#141922"
                                             Foreground="#D5DCE8" BorderThickness="0" FontSize="12"
                                             Padding="4"/>

                                    <TextBlock Text="Block a specific domain (subdomains are blocked automatically)"
                                               Style="{StaticResource FieldLabel}" Margin="0,10,0,3"/>
                                    <DockPanel>
                                        <Button x:Name="RemoveDomainButton" Content="Remove selected"
                                                Style="{StaticResource SecondaryButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="RemoveDomainButton_Click"/>
                                        <Button x:Name="AddDomainButton" Content="Block domain"
                                                Style="{StaticResource PrimaryButton}"
                                                DockPanel.Dock="Right" Margin="8,0,0,0"
                                                Click="AddDomainButton_Click"/>
                                        <TextBox x:Name="NewDomainBox" Style="{StaticResource DarkInput}"
                                                 KeyDown="NewDomainBox_KeyDown"/>
                                    </DockPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <!-- ========== SETTINGS ========== -->
                <Grid x:Name="TabSettings" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Settings" Style="{StaticResource TabTitle}"/>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Protect this computer" FontSize="15"
                                           FontWeight="SemiBold" Foreground="White"
                                           Margin="0,0,0,10"/>

                                <CheckBox x:Name="SystemDnsCheck" Style="{StaticResource DarkCheck}"
                                          Content="Route this computer's DNS through Drawbridge"
                                          Checked="SystemDnsCheck_Changed"
                                          Unchecked="SystemDnsCheck_Changed"/>

                                <CheckBox x:Name="RunAtLoginCheck" Style="{StaticResource DarkCheck}"
                                          Content="Start Drawbridge automatically at login"
                                          Checked="RunAtLoginCheck_Changed"
                                          Unchecked="RunAtLoginCheck_Changed"/>

                                <TextBlock Style="{StaticResource Hint}"
                                           Text="With both boxes checked, filtering is always on. While DNS is routed through Drawbridge, the internet only works when the bridge is up — that's the protection working."/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Remote monitoring" FontSize="15"
                                           FontWeight="SemiBold" Foreground="White"
                                           Margin="0,0,0,10"/>

                                <CheckBox x:Name="WebMonitorCheck" Style="{StaticResource DarkCheck}"
                                          Content="Enable the web monitor on this network (requires a PIN)"
                                          Checked="WebMonitorCheck_Changed"
                                          Unchecked="WebMonitorCheck_Changed"/>

                                <TextBlock x:Name="WebUrlText" Foreground="#9FE8A5" FontSize="12"
                                           TextWrapping="Wrap" Margin="0,0,0,8" Text=""/>

                                <TextBlock Style="{StaticResource Hint}"
                                           Text="Open the address above from another computer on your home network and enter the PIN. Home-network use only — don't forward this port to the internet."/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Prepare for uninstall" FontSize="15"
                                           FontWeight="SemiBold" Foreground="White"
                                           Margin="0,0,0,10"/>

                                <TextBlock Style="{StaticResource Hint}" Margin="0,0,0,10"
                                           Text="Removing Drawbridge while Windows is still pointed at it leaves the computer without working DNS. This button undoes every system change — DNS back to automatic, login task and firewall rule removed — so the app can be safely uninstalled or upgraded. Your lists, rules, PIN, and logs are kept."/>

                                <Button x:Name="CleanupButton"
                                        Content="Undo all system changes"
                                        Style="{StaticResource DangerButton}"
                                        HorizontalAlignment="Left"
                                        Click="CleanupButton_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- ========== SECURITY ========== -->
                <Grid x:Name="TabSecurity" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Security" Style="{StaticResource TabTitle}"/>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock x:Name="PinPanelTitle" Text="PIN lock" FontSize="15"
                                           FontWeight="SemiBold" Foreground="White"
                                           Margin="0,0,0,10"/>

                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" x:Name="CurrentPinPanel"
                                                Visibility="Collapsed">
                                        <TextBlock Text="Current PIN"
                                                   Style="{StaticResource FieldLabel}"/>
                                        <PasswordBox x:Name="CurrentPinBox"
                                                     Style="{StaticResource DarkPin}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="New PIN (min 4 digits)"
                                                   Style="{StaticResource FieldLabel}"/>
                                        <PasswordBox x:Name="NewPinBox"
                                                     Style="{StaticResource DarkPin}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="Confirm new PIN"
                                                   Style="{StaticResource FieldLabel}"/>
                                        <PasswordBox x:Name="ConfirmPinBox"
                                                     Style="{StaticResource DarkPin}"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button x:Name="SetPinButton" Content="Set PIN &amp; lock"
                                            Style="{StaticResource PrimaryButton}"
                                            Click="SetPinButton_Click"/>
                                    <Button x:Name="RemovePinButton" Content="Remove PIN"
                                            Style="{StaticResource SecondaryButton}"
                                            Margin="8,0,0,0" Visibility="Collapsed"
                                            Click="RemovePinButton_Click"/>
                                </StackPanel>

                                <TextBlock Style="{StaticResource Hint}" Margin="0,12,0,0"
                                           Text="Once a PIN is set, Drawbridge starts locked: only the unlock screen is visible, and the app can't be closed without the PIN. Filtering always stays active while locked. The same PIN unlocks the web monitor."/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- ========== LOGS ========== -->
                <Grid x:Name="TabLogs" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Logs" Style="{StaticResource TabTitle}"/>

                    <Border Grid.Row="1" Background="#141922" CornerRadius="8" Padding="8">
                        <ListBox x:Name="LogList" Background="Transparent" BorderThickness="0"
                                 Foreground="#9FE8A5" FontFamily="Consolas" FontSize="12"/>
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <!-- ==================== LOCK SCREEN OVERLAY ==================== -->
        <Grid x:Name="LockOverlay" Background="#1E2430" Visibility="Collapsed"
              Panel.ZIndex="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="280">
                <TextBlock Text="🔒" FontSize="52" HorizontalAlignment="Center"/>
                <TextBlock Text="Drawbridge is locked" FontSize="20" FontWeight="Bold"
                           Foreground="White" HorizontalAlignment="Center" Margin="0,8,0,2"/>
                <TextBlock Text="Filtering is still active." FontSize="12"
                           Foreground="#8A93A6" HorizontalAlignment="Center" Margin="0,0,0,18"/>

                <PasswordBox x:Name="UnlockPinBox" FontSize="18" Padding="8,6"
                             Background="#141922" Foreground="White" BorderBrush="#3B4252"
                             HorizontalContentAlignment="Center"
                             KeyDown="UnlockPinBox_KeyDown"/>

                <Button x:Name="UnlockButton" Content="Unlock" FontSize="15"
                        Style="{StaticResource PrimaryButton}" Margin="0,10,0,0"
                        Click="UnlockButton_Click"/>

                <TextBlock x:Name="UnlockError" Foreground="IndianRed" FontSize="12"
                           HorizontalAlignment="Center" Margin="0,10,0,0" Text=""/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
